# bit2bin
A lightweight, statically-linked CLI tool built in Go to convert ASCII bitstrings (e.g., "01000001") into raw binary files. Automatic padding and cross-platform builds for Linux/Windows (AMD64/ARM64).
